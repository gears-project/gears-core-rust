use structure::xflow::*;

// XXX
#[allow(unused_imports)]
use ratel::grammar::*;
use ratel::codegen::*;
use ratel::parser::parse;
use ratel::operator::OperatorKind::*;

/*
pub fn output_es5(xflow: &XFlowDocument) -> String {
    build_node_function()
}

fn build_node_function() -> String {
    let stm = Statement::Labeled {
        label: "foo".into(),
        body: Box::new(Statement::Block { body: Vec::new() }),
    };

    stm.to_code()

}
*/
